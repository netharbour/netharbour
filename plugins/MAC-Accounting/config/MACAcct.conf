# Configuration file for MAC Accounting plugin

# HTTP unauthenticated proxy support for ASN whois lookup connectivity
proxy_support = 0
proxy_address = http://xxxx.xx.xx:8080

# manually enter a description for the orgname field. Note, ASN lookup must be enabled in the plugin config first.
# 1. enable ASN lookup and be happy with the description it pulls.. OR
# 2. enable ASN lookup, but enumerate static overrides for specific entries
# format as follows...
org_desc_override = 0
m.y.i.p = route server 1
# .
# .
# .
